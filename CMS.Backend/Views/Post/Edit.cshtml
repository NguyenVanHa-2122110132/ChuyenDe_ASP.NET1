@*
    Họ và tên: Nguyễn Văn Hà
    MSSV     : 2122110132
    Lớp      : CCQ2211D
    Ngày tạo : 16/05/2026
    Mô tả    : Trang chỉnh sửa Bài viết trong khu vực Admin.
              - Hiển thị form với dữ liệu cũ được điền sẵn để chỉnh sửa
              - Tích hợp CKEditor 5 — tự nạp nội dung cũ vào trình soạn thảo nhờ asp-for="Content"
              - Bắt buộc có input hidden Id để Entity Framework biết sửa đúng dòng
              - Bắt buộc có input hidden CreatedDate để không bị mất ngày tạo khi cập nhật
              - Danh mục được load từ database xuống dropdown, tự chọn đúng danh mục cũ
              - Khi bấm Cập nhật sẽ gửi POST request đến PostController (action Edit)
              - Khi bấm Hủy bỏ sẽ trở về trang danh sách Post
*@
@model CMS.Data.Entities.Post

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Sửa bài viết";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="text-warning mb-0">SỬA BÀI VIẾT</h2>
    <a asp-action="Index" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Quay lại
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <form asp-action="Edit" method="post">
            <input type="hidden" asp-for="Id" />          @* Giữ ID để EF biết sửa đúng dòng *@
            <input type="hidden" asp-for="CreatedDate" /> @* Giữ ngày tạo không bị mất *@

            <div class="mb-3">
                <label class="form-label fw-bold">Tiêu đề:</label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Nội dung chi tiết:</label>
                @* CKEditor tự nạp nội dung cũ nhờ asp-for="Content" điền sẵn value *@
                <textarea asp-for="Content" class="form-control" rows="10"></textarea>
                <span asp-validation-for="Content" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Đường dẫn ảnh (URL):</label>
                <input asp-for="ImageUrl" class="form-control" />
                @if (Model.ImageUrl != null)
                {
                    @* Hiện preview ảnh cũ để tiện kiểm tra *@
                    <img src="@Model.ImageUrl" class="mt-2 rounded"
                         style="height:120px; object-fit:cover;" />
                }
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Danh mục:</label>
                <select asp-for="CategoryId"
                        asp-items="ViewBag.Categories"
                        class="form-select">
                    <option value="">-- Chọn danh mục --</option>
                </select>
                <span asp-validation-for="CategoryId" class="text-danger small"></span>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-warning">
                    <i class="bi bi-save"></i> Cập nhật
                </button>
                <a asp-action="Index" class="btn btn-secondary">Hủy bỏ</a>
            </div>

        </form>
    </div>
</div>

@section Scripts {
    @* Load thư viện CKEditor 5 từ CDN *@
    <script src="https://cdn.ckeditor.com/ckeditor5/39.0.1/classic/ckeditor.js"></script>

    <script>
        // Kích hoạt CKEditor cho textarea có id="Content"
        // Nội dung cũ đã được asp-for="Content" điền sẵn vào textarea
        // CKEditor sẽ tự nạp nội dung đó lên trình soạn thảo
        ClassicEditor
            .create(document.querySelector('#Content'))
            .catch(error => {
                console.error(error); // In lỗi ra console nếu CKEditor không khởi động được
            });
    </script>
}